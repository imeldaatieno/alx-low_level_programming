#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Execute
 *
 * Return: Always 0 (Success)
 *
 */
int main(void)
{
	char a, i;

	for (a = 'a'; a <= 'f'; a++)
		putchar('i');

	for (a = 'a'; a <= 'f'; a++)
		putchar(a);
	putchar('\n');
	return (0);
i}
